services:
  - type: web
    name: django-api
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      python manage.py migrate && gunicorn text_translation.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: text_translation.settings
